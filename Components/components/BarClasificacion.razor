@inject NavigationManager Navigation




        <div class="list-group w-100">
            <button type="button" class="list-group-item bg-primary p-2 disabled" aria-current="true">
                <Icon Name="IconName.Filter" IconSize="IconSize.Large" Margin="Margin.Is1" TextColor="TextColor.White" />
            </button>

            <button type="button" class="list-group-item list-group-item-action" onclick="@(()=>Navigation.NavigateTo("/ingresos/Oficinas Centrales/01"))">Sistema DIF</button>
            <button type="button" class="list-group-item list-group-item-action" onclick="@(()=>Navigation.NavigateTo("/ingresos/Junta de Asistencia/02"))">Junta de Asistencia</button>
            <button type="button" class="list-group-item list-group-item-action" onclick="@(()=>Navigation.NavigateTo("/ingresos/Hopital del Niño/03"))">Hospital Del Niño</button>
            <button type="button" class="list-group-item list-group-item-action" onclick="@(()=>Navigation.NavigateTo("/ingresos/CRIH/04"))">CRIH</button>
            <button type="button" class="list-group-item list-group-item-action" onclick="@(()=>Navigation.NavigateTo("/ingresos/Procuraduría/05"))">
                PROCURADURÍA
            </button>
        </div>
        @*Este bar sera un componente
        <BarClasificacion />*@

@*
<Bar Mode="BarMode.VerticalInline"
     CollapseMode="BarCollapseMode.Small"
     Background="Background.Light"
     ThemeContrast="ThemeContrast.Light"
     Width="Width.Max100"
    > 
    <BarBrand Class="d-flex justify-content-center align-items-center">
        <Icon Name="IconName.Filter" IconSize="IconSize.Large" Margin="Margin.Is1" />
    </BarBrand>

    <BarMenu>
        <BarStart>
           
            <BarItem>
                <BarDropdown>
                    <BarDropdownToggle TextColor="TextColor.Primary">
                        <Icon Name="IconName.CalendarTimes" IconSize="IconSize.Small" />Meses</BarDropdownToggle>
                    <BarDropdownMenu >
                        <BarDropdownItem To="/ingresos/Oficinas Centrales/01">
                            Sistema DIF
                        </BarDropdownItem>
                        <BarDropdownItem To="/ingresos/Junta de Asistencia/02">
                            Junta de Asistencia
                        </BarDropdownItem>
                        <BarDropdownItem To="/ingresos/Hopital del Niño/03">
                            Hospital Del Niño DIF
                        </BarDropdownItem>
                        <BarDropdownItem To="/ingresos/CRIH/04">
                            CRIH
                        </BarDropdownItem>
                        <BarDropdownItem To="/ingresos/Procuraduría/05">
                            @*En esta parte para que no se amontone usar las cosas de bootstrap para ocultarlo
                            PROCURADURÍA
                        </BarDropdownItem>
                        <BarDropdownDivider />
                        <BarDropdownItem>Another action</BarDropdownItem>
                    </BarDropdownMenu>
                </BarDropdown>
            </BarItem>
        </BarStart>
        <BarEnd>
             <BarItem>
            <Button Color="Blazorise.Color.Primary">Sign up</Button>
            <Button Color="Blazorise.Color.Secondary">Log in</Button>
            </BarItem>
        </BarEnd>
    </BarMenu>
</Bar>
*@
@code {
  

}
